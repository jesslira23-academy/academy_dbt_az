models:
  - name: dim_produto
    description: Tabela de dimensao para consumo final da tabela de produtos.
    columns:
      - name: pk_produto
        description: Chave primaria do produto.

      - name: ds_nome_produto
        description: "{{ doc('produto_nome') }}"

      - name: unidade_em_estoque
        description: Coluna com as informacoes de unidades em estoque.
